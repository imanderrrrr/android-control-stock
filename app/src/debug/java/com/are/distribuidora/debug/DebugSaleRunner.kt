package com.are.distribuidora.debug

import android.content.Context

/**
 * Runner temporal (solo debug).
 *
 * DESHABILITADO: el refactor de Sale eliminó los modelos/contratos legacy.
 */
object DebugSaleRunner {
    fun runOnce(applicationContext: Context) {
        // no-op
    }
}
