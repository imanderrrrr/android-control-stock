<?xml version="1.0" encoding="utf-8"?>
<!-- Ítem del carrito — rediseño "Bosque Pro". El FrameLayout raíz es para ItemTouchHelper.
     Conserva ids del adapter: cartItemCard, imageCartProduct, imageCartPlaceholder,
     textCartItemName, textCartItemSubtotal, textCartItemUnitPrice, textCartItemNotes,
     chipCartQty, chipCartDiscount. -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="20dp"
    android:layout_marginVertical="5dp">

    <com.google.android.material.card.MaterialCardView
        android:id="@+id/cartItemCard"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:cardBackgroundColor="@color/bg_card"
        app:cardCornerRadius="16dp"
        app:cardElevation="0dp"
        app:strokeColor="@color/border_subtle"
        app:strokeWidth="1dp">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="12dp">

            <com.google.android.material.card.MaterialCardView
                android:id="@+id/imageContainer"
                android:layout_width="56dp"
                android:layout_height="56dp"
                app:cardBackgroundColor="@color/bg_muted"
                app:cardCornerRadius="12dp"
                app:cardElevation="0dp"
                app:strokeWidth="0dp"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent">

                <com.google.android.material.imageview.ShapeableImageView
                    android:id="@+id/imageCartProduct"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:contentDescription="@string/product_image"
                    android:scaleType="centerCrop"
                    android:visibility="invisible" />

                <ImageView
                    android:id="@+id/imageCartPlaceholder"
                    android:layout_width="26dp"
                    android:layout_height="26dp"
                    android:layout_gravity="center"
                    android:alpha="0.5"
                    android:contentDescription="@null"
                    android:src="@drawable/ic_product_placeholder"
                    app:tint="@color/text_tertiary" />
            </com.google.android.material.card.MaterialCardView>

            <!-- Nombre -->
            <TextView
                android:id="@+id/textCartItemName"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginStart="12dp"
                android:layout_marginEnd="8dp"
                android:ellipsize="end"
                android:fontFamily="sans-serif-medium"
                android:maxLines="2"
                android:textColor="@color/text_primary"
                android:textSize="14sp"
                app:layout_constraintEnd_toStartOf="@id/rightColumn"
                app:layout_constraintStart_toEndOf="@id/imageContainer"
                app:layout_constraintTop_toTopOf="@id/imageContainer"
                tools:text="Agua Pura 600ml ×24" />

            <!-- Precio base (strikethrough conceptual, solo con descuento) -->
            <TextView
                android:id="@+id/textCartItemUnitPrice"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginStart="12dp"
                android:layout_marginEnd="8dp"
                android:layout_marginTop="2dp"
                android:fontFamily="sans-serif"
                android:maxLines="1"
                android:textColor="@color/text_tertiary"
                android:textSize="11sp"
                android:visibility="gone"
                app:layout_constraintStart_toEndOf="@id/imageContainer"
                app:layout_constraintTop_toBottomOf="@id/textCartItemName"
                tools:text="Q90.00 base"
                tools:visibility="visible" />

            <!-- Notas -->
            <TextView
                android:id="@+id/textCartItemNotes"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginStart="12dp"
                android:layout_marginEnd="8dp"
                android:layout_marginTop="3dp"
                android:ellipsize="end"
                android:fontFamily="sans-serif"
                android:maxLines="2"
                android:textColor="@color/text_secondary"
                android:textSize="11sp"
                android:textStyle="italic"
                android:visibility="gone"
                app:layout_constraintStart_toEndOf="@id/imageContainer"
                app:layout_constraintEnd_toStartOf="@id/rightColumn"
                app:layout_constraintTop_toBottomOf="@id/textCartItemUnitPrice"
                tools:text="Empacado triple"
                tools:visibility="visible" />

            <!-- Columna derecha: total + cantidad + descuento -->
            <LinearLayout
                android:id="@+id/rightColumn"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:gravity="end"
                android:minWidth="56dp"
                android:orientation="vertical"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintTop_toTopOf="parent">

                <TextView
                    android:id="@+id/textCartItemSubtotal"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:fontFamily="sans-serif"
                    android:maxLines="1"
                    android:textColor="@color/brand_accent"
                    android:textSize="15sp"
                    android:textStyle="bold"
                    tools:text="Q72.00" />

                <TextView
                    android:id="@+id/chipCartQty"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="6dp"
                    android:background="@drawable/bg_pill"
                    android:backgroundTint="@color/brand_soft"
                    android:fontFamily="sans-serif"
                    android:gravity="center"
                    android:paddingHorizontal="10dp"
                    android:paddingVertical="3dp"
                    android:textColor="@color/brand_soft_text"
                    android:textSize="12sp"
                    android:textStyle="bold"
                    tools:text="× 24" />

                <TextView
                    android:id="@+id/chipCartDiscount"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="4dp"
                    android:background="@drawable/bg_pill"
                    android:backgroundTint="@color/warning_bg"
                    android:fontFamily="sans-serif"
                    android:gravity="center"
                    android:paddingHorizontal="9dp"
                    android:paddingVertical="2dp"
                    android:textColor="@color/warning_text"
                    android:textSize="10sp"
                    android:textStyle="bold"
                    android:visibility="gone"
                    tools:text="-10%"
                    tools:visibility="visible" />
            </LinearLayout>

        </androidx.constraintlayout.widget.ConstraintLayout>

    </com.google.android.material.card.MaterialCardView>

</FrameLayout>
