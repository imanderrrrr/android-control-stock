<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingVertical="8dp">

    <!-- Ranking badge -->
    <TextView
        android:id="@+id/textRank"
        android:layout_width="28dp"
        android:layout_height="28dp"
        android:background="@drawable/bg_rank_badge"
        android:gravity="center"
        android:textColor="@color/app_on_primary"
        android:textSize="13sp"
        android:textStyle="bold"
        tools:text="1" />

    <!-- Product image (hidden by default, shown for products) -->
    <com.google.android.material.imageview.ShapeableImageView
        android:id="@+id/imageProduct"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_marginStart="10dp"
        android:scaleType="centerCrop"
        android:visibility="gone"
        app:shapeAppearanceOverlay="@style/ShapeAppearanceOverlay.Distribuidora.ImageRounded"
        tools:visibility="visible" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="10dp"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/textName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:maxLines="1"
            android:textColor="@color/app_on_surface"
            android:textSize="14sp"
            android:textStyle="bold"
            tools:text="Coca Cola 600ml" />

        <TextView
            android:id="@+id/textSubtitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/palette_600"
            android:textSize="12sp"
            tools:text="42 uds · Q 1,260.00" />
    </LinearLayout>

    <!-- Bar visual (proporción) -->
    <View
        android:id="@+id/barProportion"
        android:layout_width="60dp"
        android:layout_height="8dp"
        android:layout_marginStart="8dp"
        android:background="@color/app_primary" />
</LinearLayout>

